const express = require('express');
const app = express();

// Settings

// Middlewares - Funciones que se ejecutan antes de que llegen a las rutas

// Routes

// Static files - Donde van los archivos estaticos los html los css lo javascrip

//Starting the server


app.listen(3000, () => {
    console.log('server on port 3000');
});